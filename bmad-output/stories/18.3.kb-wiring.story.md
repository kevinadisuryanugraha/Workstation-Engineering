<!--
  STORY CONTEXT OBJECT — bmad-epics-and-stories (via bmad-correct-course CC-5)
  File name: 18.3.kb-wiring.story.md

  This file is SELF-CONTAINED: a dev agent should need nothing else.
  LOCKED SECTIONS: Acceptance Criteria, Dev Notes, and Testing are LOCKED.
  External dev tools MUST NOT edit them — only the planning skill may, via
  explicit Update + decision-log entry. The dev tool fills ONLY "Dev Agent Record".
-->

# 18.3: KB Wiring — Hook + Hydration View Knowledge Base

**Story ID:** 18.3
**Epic:** 18 — Client API Wiring: Honest Data Everywhere (Course Correction 5)
**Slug:** kb-wiring
status: ready-for-dev   <!-- lifecycle: backlog -> ready-for-dev -> in-progress -> review -> done -->

> Owned by planning until `ready-for-dev`. Sized to <half dev-day. No story points.

## Story

As a **pengguna WORKSTATION di boot produksi**,
I want **tab Knowledge Runbooks menampilkan artikel KB nyata (hasil versioning 15.1 & draft dari tiket resolved)**,
so that **runbook yang dibaca tim saat insiden adalah konten terkurasi nyata — bukan artikel demo**.

## Acceptance Criteria

<!-- LOCKED. Numbered, testable. External dev tools must not edit. -->

1. Client memakai React Query ke endpoint list KB eksisting (`GET /api/v1/kb/...` — pilih endpoint list yang sesuai) dengan gating `!DEMO_MODE && isAuth`.
2. Boot real: `KnowledgeBaseView` menampilkan artikel nyata; honest-empty notice hilang saat data ada. Mode demo: seed mock utuh.
3. Mapper DTO→UI `KnowledgeArticle` = pure function testable; field tanpa padanan memakai placeholder jujur.
4. Endpoint server TIDAK diubah (murni client).
5. `npm run lint` (tsc strict) dan seluruh test tetap hijau; test baru untuk mapper.

## Tasks / Subtasks

- [ ] Task 1: `src/hooks/api/useKbArticles.ts` (BARU) — opsi `enabled` (pola `useSprints`)
- [ ] Task 2: Mapper DTO→UI + hydration di blok sumber data `App.tsx`
- [ ] Task 3: Test `tests/kb-client.test.ts` (BARU)
- [ ] Task 4: Verifikasi lint + full test + build

## Dev Notes

- **Endpoint:** `server/modules/kb/kb.routes.ts` punya beberapa GET (list, by slug, versions) — pilih yang list publikasi untuk feed view; cek `kb.service.ts` untuk bentuk row (slug, versi, tags, dsb.).
- **UI `KnowledgeArticle`** (src/types.ts) punya field tampilan (tags[], content) — mapping jujur; artikel draft vs published: ikuti semantik endpoint yang dipilih.
- **Terkait:** 15.1 (versioning + draft dari tiket resolved) · 18.2 (sebelumnya) · 18.4 (sesudahnya).

## Testing

- Unit test mapper; contract demo/real; regression penuh hijau.

## Dependency Maps

- **Depends on:** 18.2 (sequential — shared `App.tsx`).
- **Blocks:** 18.4.

## Owned File/Module Scope

- `src/hooks/api/useKbArticles.ts` (BARU)
- `src/App.tsx` (blok sumber data + mapper saja)
- `tests/kb-client.test.ts` (BARU)

## Learnings from Previous Stories

- Dari 17.2: gating `enabled`; mapper pure; placeholder jujur.

## Dev Agent Record

<!-- Diisi oleh dev tool eksternal. JANGAN mengedit section LOCKED di atas. -->

- Status:
- Commit:
- Catatan:
