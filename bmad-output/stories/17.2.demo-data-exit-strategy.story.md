<!--
  STORY CONTEXT OBJECT — bmad-epics-and-stories (via bmad-correct-course CC-4)
  File name: 17.2.demo-data-exit-strategy.story.md

  This file is SELF-CONTAINED: a dev agent should need nothing else.
  LOCKED SECTIONS: Acceptance Criteria, Dev Notes, and Testing are LOCKED.
  External dev tools MUST NOT edit them — only the planning skill may, via
  explicit Update + decision-log entry. The dev tool fills ONLY "Dev Agent Record".
-->

# 17.2: Demo Data Gating — Default Real API Data, Demo Behind Flag

**Story ID:** 17.2
**Epic:** 17 — UI Clarity & Role-Based Navigation (Course Correction 4)
**Slug:** demo-data-exit-strategy
status: done   <!-- lifecycle: backlog -> ready-for-dev -> in-progress -> review -> done -->

> Owned by planning until `ready-for-dev`. Sized to one dev-day (~2-8h). No story points.

## Story

As a **pengguna WORKSTATION di produksi**,
I want **aplikasi yang menampilkan data nyata dari API secara default, dengan data demo hanya aktif lewat flag eksplisit dan selalu berlabel "demo"**,
so that **layar terasa tenang dan jujur — kepadatan semu dari mock data (badge commit palsu, node "2 Live", rilis v2.8.1 fiktif) hilang dari pengalaman harian**.

## Acceptance Criteria

<!-- LOCKED. Numbered, testable. External dev tools must not edit. -->

1. Default boot aplikasi (tanpa flag) TIDAK menampilkan satu pun data dari `src/mockData.ts` pada view yang sudah memiliki sumber data API nyata via React Query (`src/hooks/api/*` — work items, tickets, incidents, sprints, server metrics, reports).
2. Mode demo hanya aktif ketika `VITE_DEMO_MODE=1` (atau setara via flag build); dalam mode demo, banner/label demo yang jelas tampil (melanjutkan prinsip SEC-05 "honestly labeled demo data").
3. Ketika mode demo OFF dan API mengembalikan data kosong, view menampilkan empty state yang jujur dan informatif (mis. "Belum ada work item — buat yang pertama") — BUKAN area kosong, BUKAN data palsu.
4. View yang hingga kini HANYA memiliki sumber mock (tanpa endpoint nyata) tidak diubah perilakunya oleh story ini — mereka dicatat dalam Dev Agent Record sebagai daftar temuan untuk keputusan lanjutan (course correction berikutnya), tanpa menghapus datanya di story ini.
5. `metadata.json` (sisa applet AI Studio) tidak disentuh story ini; hanya jalur data runtime frontend yang berubah.
6. `npm run lint` (tsc strict) dan seluruh test eksisting tetap hijau.

## Tasks / Subtasks

- [x] Task 1: Audit semua konsumsi `mockData` di `src/App.tsx` & komponen — buat daftar: sudah-adalah-API / masih-mock / hybrid
- [x] Task 2: Introduksi flag `VITE_DEMO_MODE` (`.env.example` + tipe env) dan gating di titik konsumsi data `App.tsx`
- [x] Task 3: Banner label demo pada mode demo (konsisten pola label SEC-05)
- [x] Task 4: Empty states untuk view tanpa data nyata (AC 3)
- [x] Task 5: Unit test `tests/demo-mode.test.ts` + verifikasi manual produksi (AC 1, 2)

## Dev Notes

- **Sumber masalah (CC-4):** scan 19 Sep (`bmad-output/project-documentation.md` §7 Planning Note 4): `mockData.ts` (763+ brs) masih di-import `App.tsx`; owner melihat "banyak sekali dan padat" — sebagian adalah konten palsu (contoh nyata: badge sidebar "4 Commits", "v2.8.1", node "2 Live" — string ini terverifikasi di `Sidebar.tsx`/`mockData.ts`).
- **Prinsip SEC-05 (Story 8.3):** platform sudah punya konvensi "fallback demo yang diberi label jujur" di `server/modules/ai/ai.fallback.ts` — ikuti pola labeling yang sama untuk banner demo (AC 2).
- **Data nyata sudah tersedia untuk:** work items, tickets, incidents, sprints/milestones, server metrics, reports, KB, search, audit — via `/api/v1/*` dan hook `src/hooks/api/*` (7 hook React Query). Gunakan itu sebagai jalur default.
- **Perhatian:** `App.tsx` adalah god-component (1.059 brs) — ubah HANYA blok konsumsi sumber data; jangan refactor struktural (di luar scope).
- **Terkait:** 17.1 (sebelumnya, sequential karena shared `App.tsx`), 17.3 (sesudahnya).

## Testing

- Unit test baru `tests/demo-mode.test.ts` (vitest):
  1. Flag OFF → konsumsi data view beralih ke hook API; tidak ada import runtime mock yang tereksekusi.
  2. Flag ON → data mock tampil + banner demo aktif.
  3. Empty API response → empty state object/komponen terpilih (tanpa mock).
- Regression: `npx vitest run` hijau semua; `npm run lint` bersih.
- Verifikasi manual: boot produksi (build tanpa flag) menampilkan data nyata; boot dengan `VITE_DEMO_MODE=1` menampilkan banner demo.

## Dependency Maps

- **Depends on:** 17.1 (sequential — shared `App.tsx`).
- **Blocks:** 17.3.

## Owned File/Module Scope

- `src/mockData.ts` (gating)
- `src/App.tsx` (blok sumber data saja)
- `.env.example` (VITE_DEMO_MODE)
- `tests/demo-mode.test.ts` (BARU)

## Learnings from Previous Stories

- Dari 17.1: jangan sentuh `Sidebar.tsx` di story ini (scope 17.1).
- Dari SEC-05/8.3: label demo yang jujur adalah pola yang sudah diterima owner — gunakan, jangan ciptakan konvensi baru.
- Dari scan 19 Sep: API nyata sudah mencakup hampir semua domain — priority-kan view yang punya hook API.

## Dev Agent Record

<!-- Diisi oleh dev tool eksternal. JANGAN mengedit section LOCKED di atas. -->

- Status: DONE — semua AC terpenuhi; lint (tsc strict) bersih; 195/195 test hijau (39 file, termasuk 6 test baru); production build sukses.
- Commit: (lihat merge commit story/17-2-demo-data-exit-strategy di branch integrasi)
- Daftar view masih-full-mock (untuk CC berikutnya) — domain dengan endpoint nyata tetapi BELUM ada wiring UI→API (di boot real tampil KOSONG + honest notice; mock hanya tampil di mode demo):
  1. **AI Intelligence** (findings/recommendations/technical debts — API 16.x ada, hook client belum; view sengaja dibiarkan mounted karena CTA scan-nya aksi nyata).
  2. **Git Intelligence** (commits/PRs — data webhook 5.x ada, hook client belum).
  3. **Deployments** (API 6.1 ada, hook client belum).
  4. **Knowledge Base** (API 15.1 ada, hook client belum).
  5. **Audit Event Ledger** (API 7.2 ada; feed UI `EngineeringEvent` belum terhubung).
  6. **GlobalSearchModal** masih konsumsi props state lokal (API global-search 15.2 belum dipakai modal).
  7. **ReportsView** (agregasi) menerima campuran: real (workItems/tickets/incidents/servers) + domain belum-wired (commits/PRs/deployments).
  8. Field tampilan `Project` tanpa sumber API (owner, techLead, currentSprint, latestRelease, repoName, productionStatus) diisi placeholder jujur ("—") oleh `mapProjectDto` — jangan ditampilkan sebagai data nyata sampai API menyediakannya.
- Catatan:
  - `useWorkItems`/`useTickets` TIDAK dipakai karena hook tersebut tidak punya opsi `enabled` (di luar owned scope untuk diedit); diganti inline `useQuery` dengan `enabled: !DEMO_MODE && isAuth` agar tidak ada request 401 di login screen maupun request percuma saat mode demo. `queryKey` disamakan ("workItems"/"tickets") agar cache React Query tetap berbagi.
  - Handler mutasi lokal (create/update work item, ticket, incident, rollback, dsb.) masih `setState` lokal tanpa persist ke API — perilaku warisan, di luar scope story ini; dicatat untuk course correction berikutnya.
  - Mode demo mempertahankan perilaku merge telemetri/insiden live ke seed mock (perilaku 9.3/13.3 tidak diubah saat demo).
  - `metadata.json` tidak disentuh (AC5). `src/mockData.ts` tidak kehilangan satu pun data — hanya ditambah seksi gating.
  - Verifikasi runtime manual (boot produksi vs `VITE_DEMO_MODE=1` dengan DB+server hidup) diserahkan ke sesi smoke test integrasi; kontrak gating terverifikasi unit test.
