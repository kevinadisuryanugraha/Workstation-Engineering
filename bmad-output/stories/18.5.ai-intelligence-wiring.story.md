<!--
  STORY CONTEXT OBJECT — bmad-epics-and-stories (via bmad-correct-course CC-5)
  File name: 18.5.ai-intelligence-wiring.story.md

  This file is SELF-CONTAINED: a dev agent should need nothing else.
  LOCKED SECTIONS: Acceptance Criteria, Dev Notes, and Testing are LOCKED.
  External dev tools MUST NOT edit them — only the planning skill may, via
  explicit Update + decision-log entry. The dev tool fills ONLY "Dev Agent Record".
-->

# 18.5: AI Intelligence Wiring — Findings & Recommendations dari API

**Story ID:** 18.5
**Epic:** 18 — Client API Wiring: Honest Data Everywhere (Course Correction 5)
**Slug:** ai-intelligence-wiring
status: ready-for-dev   <!-- lifecycle: backlog -> ready-for-dev -> in-progress -> review -> done -->

> Owned by planning until `ready-for-dev`. Sized to one dev-day. No story points.

## Story

As a **pengguna WORKSTATION**,
I want **tab AI Code Scanner menampilkan findings & recommendations nyata hasil scan tersimpan (Epic 16)**,
so that **analisis kode yang saya tinjau adalah hasil scan persisten yang tervalidasi manusia — bukan temuan demo statis**.

## Acceptance Criteria

<!-- LOCKED. Numbered, testable. External dev tools must not edit. -->

1. Client memakai React Query ke endpoint eksisting `GET /api/v1/ai/...` (list findings + list recommendations) dengan gating `!DEMO_MODE && isAuth`.
2. Boot real: `AIIntelligenceView` menampilkan findings/recommendations nyata; badge sidebar `aiFindingsCount` (PENDING) kini menghitung data nyata. Mode demo: seed mock utuh.
3. Mapper DTO→UI (`AIFinding`, `AIRecommendation`) = pure function testable; placeholder jujur untuk field tanpa padanan.
4. `technicalDebts` TIDAK memiliki endpoint di `ai-intel.routes.ts` — dibiarkan kosong di boot real (honest), dicatat di Dev Agent Record sebagai kebutuhan endpoint masa depan; JANGAN mengarang data.
5. Aksi eksisting view (update status finding, konversi rekomendasi, run scan) TIDAK diubah perilaku view-nya — story ini fokus pada jalur BACA.
6. `npm run lint` (tsc strict) dan seluruh test tetap hijau; test baru untuk mapper.

## Tasks / Subtasks

- [x] Task 1: `src/hooks/api/useAiIntel.ts` (BARU) — findings + recommendations, opsi `enabled`
- [x] Task 2: Mapper DTO→UI + hydration di blok sumber data `App.tsx` (dua state: `aiFindings`, `aiRecommendations`; `technicalDebts` tetap dari gating)
- [x] Task 3: Test `tests/ai-client-wiring.test.ts` (BARU)
- [x] Task 4: Verifikasi lint + full test + build

## Dev Notes

- **Endpoint:** `server/modules/ai/ai-intel.routes.ts` — GET scans (2), GET findings, PATCH finding status, GET recommendations, POST convert (baris ±22/36/49/62/89/108). Pilih endpoint LIST yang sesuai; verifikasi bentuk payload di controller.
- **Konsistensi SEC-05:** view punya badge `scanMode` (STATIC_DEMO_PREVIEW vs LIVE_ANALYSIS) — data dari API adalah hasil scan tersimpan; biarkan badge mengikuti respons backend, jangan mengarang mode.
- **Sidebar badge:** `aiFindingsCount` di App otomatis jadi jujur begitu state ter-hydrate.
- **Terkait:** 16.1–16.4 (snapshot, lifecycle, konversi, translate) · 18.4 (sebelumnya) · 18.6 (sesudahnya).

## Testing

- Unit test mapper (finding + recommendation); contract demo/real; technicalDebts tetap kosong di real.
- Regression: full vitest hijau; lint bersih.

## Dependency Maps

- **Depends on:** 18.4 (sequential — shared `App.tsx`).
- **Blocks:** 18.6.

## Owned File/Module Scope

- `src/hooks/api/useAiIntel.ts` (BARU)
- `src/App.tsx` (blok sumber data + mapper saja)
- `tests/ai-client-wiring.test.ts` (BARU)

## Learnings from Previous Stories

- Dari 8.3/SEC-05: label mode demo harus jujur — jangan pertaruhkan data demo ke jalur LIVE.
- Dari 16.2: lifecycle finding server-side — client hanya membaca status yang ada.

## Dev Agent Record

<!-- Diisi oleh dev tool eksternal. JANGAN mengedit section LOCKED di atas. -->

- Status: DONE — AC1–AC6 terpenuhi. Hook `useAiFindings`/`useAiRecommendations` (gated enabled) + mapper normalisasi kasus dengan fallback jujur. Lint bersih; **221/221 test hijau** (44 file — 6 test baru); build sukses. Endpoint server 16.x TIDAK disentuh.
- Commit: (lihat merge commit story/18-5-ai-intelligence-wiring di branch integrasi)
- Catatan:
  - Nilai di luar union (severity/category/status aneh dari DB) → fallback netral (Medium/PENDING/Architecture) — bukan error, bukan karangan berbahaya.
  - Field nullable → placeholder jujur (affectedFile "—", string lain ""); `convertedToWorkItem` hanya terisi bila `convertedWorkItemKey` nyata.
  - **technicalDebts tetap kosong di boot real** (AC4) — belum ada endpoint; kandidat: endpoint technical debt di CC berikutnya.
  - Badge sidebar `aiFindingsCount` kini menghitung data nyata (PENDING dari API).
  - Permission endpoint: `PERM_AI_SCAN_TRIGGER` — role tanpa permission menerima 403 → error state React Query (view juga sudah tergating nav 17.1).
